# TODO

- Mobile: Comms panel send button goes off-screen when switching to a different GS; virtual keyboard shifts layout and breaks the UI.
- Add a Target object type (so Flank can target a target entity, not just a WP/Home).
